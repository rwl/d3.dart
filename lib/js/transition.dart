/// A transition is a special type of selection where the operators apply
/// smoothly over time rather than instantaneously.
library d3.js.transition;

export '../src/js/transition.dart' hide newTransition, getProxy;
